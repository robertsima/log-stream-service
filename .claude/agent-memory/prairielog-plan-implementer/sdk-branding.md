---
name: sdk-branding-prairielog
description: SDK/package branding decision for client SDK work.
metadata:
  type: project
---

Client SDK work should use the PrairieLog brand and package naming such as `@prairielog/client`.

**Why:** On 2026-07-02, the user chose "PrairieLog" when asked to resolve the blocker between "Log Stream Service" and "PrairieLog" before npm-shaped SDK work.
**How to apply:** Use PrairieLog in SDK names, docs, examples, and package metadata unless the user later explicitly renames the product.
