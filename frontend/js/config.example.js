// Copy to config.js and set your deployed backend URL before publishing the static frontend.
window.CONFIG = {
  API_BASE_URL: "https://your-backend.example.com",
  OPENAPI_PATH: "./resources/openapi.json"
};
