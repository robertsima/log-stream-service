spring:
  application:
    name: log-stream-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/appdb}
    username: ${DB_USER:appuser}
    password: ${DB_PASSWORD:apppassword}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog.xml
  jpa:
    open-in-view: false

server:
  port: ${PORT:8080}
  servlet:
    session:
      persistent: false

springdoc:
  enable-data-rest: false
  packages-to-scan: com.logstream.controller
  swagger-ui:
    enabled: ${SWAGGER_UI_ENABLED:false}
  api-docs:
    enabled: ${SWAGGER_UI_ENABLED:false}

app:
  cors:
    allowed-origins: "${ALLOWED_ORIGINS:https://prairie-log-api.netlify.app,http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://localhost:8000,http://127.0.0.1:8000}"
  security:
    jwt-enabled: ${JWT_ENABLED:false}
    auth-enabled: ${AUTH_ENABLED:false}
    firebase-project-id: ${FIREBASE_PROJECT_ID:}
    demo-bypass-enabled: ${DEMO_BYPASS_ENABLED:false}
    demo-bypass-email: ${DEMO_BYPASS_EMAIL:admin@email.com}
    demo-jwt-secret: ${DEMO_JWT_SECRET:}
  quotas:
    max-apps-per-user: ${APP_QUOTAS_MAX_APPS_PER_USER:10}
    max-active-tokens-per-app: ${APP_QUOTAS_MAX_ACTIVE_TOKENS_PER_APP:5}
  rate-limit:
    management-requests-per-minute: ${APP_RATE_LIMIT_MANAGEMENT_RPM:60}

alerts:
  enabled: ${ALERTS_ENABLED:true}
  aggregation-window-ms: ${ALERTS_AGGREGATION_WINDOW_MS:60000}
  max-messages-per-alert: ${ALERTS_MAX_MESSAGES_PER_ALERT:5}
